import meta::pure::persistence::metamodel::*;
import meta::pure::persistence::metamodel::trigger::*;

/**********
 * context
 **********/

Class
{doc.doc = 'Details for executing a persistence specification using the AWS Glue ETL service: https://docs.aws.amazon.com/glue/index.html'}
meta::pure::persistence::aws::metamodel::AwsGluePersistenceContext extends PersistenceContext
{
}

/**********
 * trigger
 **********/

Class
{doc.doc = 'A trigger that executes an AWS Glue job on a schedule specified as a cron expression: https://docs.aws.amazon.com/glue/latest/dg/monitor-data-warehouse-schedule.html'}
meta::pure::persistence::aws::metamodel::trigger::AwsGlueScheduledTrigger extends Trigger
{
   minutes: String[1];
   hours: String[1];
   dayOfMonth: String[1];
   month: String[1];
   dayOfWeek: String[1];
   year: String[1];
}
